# 요구사항 수행 내역서 — 시스템 관제 자동화

## 1. SSH

```bash
sudo sed -i 's/^#\?Port .*/Port 20022/' /etc/ssh/sshd_config
sudo sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin no/' /etc/ssh/sshd_config
sudo systemctl restart ssh
ss -tulnp | grep ":20022"
```

**확인**:
- `Port 20022` ✓
- `PermitRootLogin no` ✓
- `ss -tulnp | grep 20022` 에 sshd LISTEN ✓

## 2. 방화벽 (UFW 선택)

```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 20022/tcp
sudo ufw allow 15034/tcp
sudo ufw enable
sudo ufw status verbose
```

**확인**:
- 20022/tcp ALLOW ✓
- 15034/tcp ALLOW ✓
- 그 외 INBOUND DENY ✓

## 3. 계정/그룹

| 계정 | 그룹 |
|---|---|
| agent-admin | agent-common, agent-core |
| agent-dev | agent-common, agent-core |
| agent-test | agent-common |

```bash
id agent-admin
# uid=1001 ... groups=1001(agent-admin),1002(agent-common),1003(agent-core)
```

## 4. 디렉토리 구조

| 경로 | owner:group | 권한 |
|---|---|---|
| `$AGENT_HOME` | agent-admin:agent-admin | 755 |
| `$AGENT_HOME/upload_files` | agent-admin:**agent-common** | **770** |
| `$AGENT_HOME/api_keys` | agent-admin:**agent-core** | **770** |
| `/var/log/agent-app` | agent-admin:**agent-core** | **770** |

## 5. 환경변수 / 키 파일

```bash
export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
export AGENT_KEY_PATH=$AGENT_HOME/api_keys/secret.key
export AGENT_LOG_DIR=/var/log/agent-app
export MEMORY_LIMIT=256
export CPU_MAX_OCCUPY=90
export MULTI_THREAD_ENABLE=true
```

키 파일: `$AGENT_HOME/api_keys/secret.key` (내용: `agent_api_key_test`).

## 6. 앱 부트 시퀀스

```
[1/5] Checking User Account               [OK]
[2/5] Verifying Environment Variables     [OK]
[3/5] Checking Required Files             [OK]
[4/5] Checking Port Availability          [OK]
[5/5] Verifying Log Permission            [OK]
Agent READY
```

## 7. monitor.sh 실행 결과

```
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent_app.py'... [OK] (PID: 48291)
Checking port 15034... [OK]

[RESOURCE MONITORING]
CPU Usage : 25.3%
MEM Usage : 5.2%
DISK Used : 23%

[WARNING] CPU threshold exceeded (25.3% > 20%)

[INFO] Log appended: /var/log/agent-app/monitor.log
```

## 8. /var/log/agent-app/monitor.log (cron 누적)

```
[2026-05-07 14:00:01] PID:48291 CPU:10.2% MEM:3.2% DISK_USED:23%
[2026-05-07 14:01:01] PID:48291 CPU:18.7% MEM:5.0% DISK_USED:23%
[2026-05-07 14:02:01] PID:48291 CPU:25.3% MEM:9.8% DISK_USED:23%
```

## 9. crontab 등록 (agent-admin)

```bash
sudo crontab -u agent-admin -l
# * * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/cron.log 2>&1
```

매분 실행 → 1~2분 후 monitor.log 누적 라인 추가 확인 ✓
